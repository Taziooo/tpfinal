<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{% block title %}Dessine-moi un site{% endblock %}</title>

  {% block stylesheets %}
    <script src="https://cdn.tailwindcss.com"></script>
  {% endblock %}
</head>
<body class="bg-gray-100 min-h-screen">

<nav class="bg-white shadow">
  <div class="max-w-5xl mx-auto px-6 py-4 flex gap-6">
    <a class="font-semibold hover:underline" href="{{ path('app_home') }}">Accueil</a>
    {# Les autres liens arrivent après création des routes #}
  </div>
</nav>

<main class="max-w-5xl mx-auto px-6 py-10">
  {% for label, messages in app.flashes %}
    {% for message in messages %}
      <div class="mb-4 p-4 rounded bg-white shadow">
        <strong>{{ label|upper }}</strong> : {{ message }}
      </div>
    {% endfor %}
  {% endfor %}

  {% block body %}{% endblock %}
</main>

</body>
</html>
