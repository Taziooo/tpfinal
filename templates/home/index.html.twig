{% extends 'base.html.twig' %}
{% block title %}Accueil — Goodies Shop{% endblock %}

{% block body %}
  <div class="card card-pad">
    <h1 class="h1">Mini e-commerce Symfony.</h1>
    <p class="muted">Catalogue + panier + commande. Paiement simulé. Légal, simple, rendu propre.</p>

    <div class="sep"></div>

    <div class="kpi">
      <div class="pill">✅ Auth</div>
      <div class="pill">✅ CRUD Produits</div>
      <div class="pill">✅ Panier session</div>
      <div class="pill">✅ Checkout simulé</div>
    </div>

    <div style="margin-top:16px; display:flex; gap:10px; flex-wrap:wrap">
      <a class="btn btn-primary" href="{{ path('app_shop_index') }}">Voir la boutique</a>
      <a class="btn" href="{{ path('app_cart_index') }}">Voir le panier</a>
    </div>
  </div>
{% endblock %}
