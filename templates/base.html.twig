<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>{% block title %}Goodies Shop{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ asset('assets/app.css') }}">
  </head>
  <body>
    <header class="nav">
      <div class="nav-inner">
        <a class="brand" href="{{ path('app_home') }}">
          <span>🛒</span>
          <span>Goodies Shop</span>
          <span class="badge">paiement simulé</span>
        </a>

        <nav class="nav-links">
          <a href="{{ path('app_home') }}">Accueil</a>
          <a href="{{ path('app_shop_index') }}">Boutique</a>
          <a href="{{ path('app_cart_index') }}">Panier</a>
        </nav>

        <div class="nav-right">
          {% if app.user %}
            <span class="small">{{ app.user.userIdentifier }}</span>
            {% if is_granted('ROLE_ADMIN') %}
              <a class="btn" href="/admin/products">Admin</a>
            {% endif %}
            <a class="btn" href="{{ path('app_logout') }}">Déconnexion</a>
          {% else %}
            <a class="btn btn-primary" href="{{ path('app_login') }}">Connexion</a>
          {% endif %}
        </div>
      </div>
    </header>

    <main class="container">
      {% for key, messages in app.flashes %}
        {% for message in messages %}
          <div class="flash">
            <strong>{{ key|upper }}</strong> — {{ message }}
          </div>
        {% endfor %}
      {% endfor %}

      {% block body %}{% endblock %}
    </main>

    <footer class="footer">
      <div>© {{ "now"|date("Y") }} Goodies Shop — site démo (pas de vente réelle).</div>
    </footer>
  </body>
</html>
