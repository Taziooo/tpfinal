{% extends 'base.html.twig' %}
{% block title %}{{ product.name }}{% endblock %}

{% block body %}
  <h1 class="text-2xl font-bold mb-2">{{ product.name }}</h1>
  <p class="text-gray-700 mb-2">{{ product.description }}</p>
  <p class="font-semibold mb-4">{{ product.priceEuros }} €</p>

  <form method="post" action="{{ path('app_cart_add', {id: product.id}) }}" class="flex gap-2 items-center">
    <input type="number" name="qty" min="1" value="1" class="border rounded p-2 w-24">
    <button class="bg-black text-white px-4 py-2 rounded" type="submit">Ajouter au panier</button>
  </form>
{% endblock %}
